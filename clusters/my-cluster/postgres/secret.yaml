apiVersion: v1
kind: Secret
metadata:
  name: postgres-auth
  namespace: database
  annotations:
    kubernetes.io/description: "Autogenerated placeholder; replace values before committing to production."
type: Opaque
stringData:
  postgres-password: change-me
  # Optional: application user password, uncomment to sync with chart values.
  # password: change-me-too
