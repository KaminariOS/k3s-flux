apiVersion: apps/v1
kind: Deployment
metadata:
  name: sciflow-images
  namespace: sciflow
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: images
      app.kubernetes.io/part-of: sciflow
  template:
    metadata:
      labels:
        app.kubernetes.io/name: images
        app.kubernetes.io/part-of: sciflow
    spec:
      serviceAccountName: sciflow-operations
      imagePullSecrets:
        - name: sciflow-ghcr
      nodeSelector:
        kubernetes.io/arch: amd64
      volumes:
        - name: registry-auth
          secret:
            secretName: sciflow-registry-auth
      containers:
        - name: images
          image: ghcr.io/yuanwuzhi/sciflow-images:sha-c24fc3e729c4
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: sciflow-config
            - secretRef:
                name: sciflow-db
          env:
            - name: BIND_ADDR
              value: 0.0.0.0:8080
            - name: POLICY_BASE_URL
              value: http://10.43.104.223
          ports:
            - name: http
              containerPort: 8080
          volumeMounts:
            - name: registry-auth
              mountPath: /run/sciflow/registry-auth
              readOnly: true
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
---
apiVersion: v1
kind: Service
metadata:
  name: sciflow-images
  namespace: sciflow
spec:
  selector:
    app.kubernetes.io/name: images
    app.kubernetes.io/part-of: sciflow
  ports:
    - name: http
      port: 80
      targetPort: http
